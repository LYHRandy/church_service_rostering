import { describe, expect, it } from 'vitest';

// Placeholder until real units land (T5+). Keeps `npm test` meaningful in CI from day one.
describe('test harness', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
